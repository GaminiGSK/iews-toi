---
description: Automatically deploy changes to GitHub
---

// turbo-all

1. Add all changes
```powershell
git add .
```

2. Commit changes
```powershell
git commit -m "chore: auto-deploy changes"
```

3. Push to remote
```powershell
git push
```
